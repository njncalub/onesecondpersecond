<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ renderData.title or title or metadata.title }} | {{ metadata.site.title }}</title>
    <meta name="description" content="{{ renderData.description or description or metadata.description }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <meta name="theme-color" content="#fafafa" />

    <link rel="stylesheet" href="{{ '/static/css/normalize.css' | url }}" />
    <link rel="stylesheet" href="{{ '/static/css/h5bp.css' | url }}" />
    <link rel="stylesheet" href="{{ '/static/css/11ty.css' | url }}" />
    <link rel="stylesheet" href="{{ '/static/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="stylesheet" href="{{ '/static/css/index.css' | url }}" />
    
    <link rel="alternate"
          href="{{ metadata.feed.path | url }}"
          type="application/atom+xml"
          title="{{ metadata.site.title }}" />
  </head>

  <body>
    <!--[if IE]>
      <p class="browserupgrade">
        You are using an <strong>outdated</strong> browser. Please
        <a href="https://browsehappy.com/">upgrade your browser</a> to improve
        your experience and security.
      </p>
    <![endif]-->
    
    <header>
      <h1 class="home"><a href="{{ '/' | url }}">{{ metadata.site.title }}</a></h1>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
      </ul>
    </header>
    
    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>
    
    <footer></footer>
    
    <script src="{{ '/static/js/index.js' | url }}"></script>
  </body>
</html>
